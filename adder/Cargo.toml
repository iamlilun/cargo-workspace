[package]
name = "adder"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# workspace並不會假定庫之前彼此依賴
# 所以要顯示的用path指向依賴
add_one = { path = "../add_one"}
rand = "0.3.15"